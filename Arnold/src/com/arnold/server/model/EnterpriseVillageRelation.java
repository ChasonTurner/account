package com.arnold.server.model;

import java.util.ArrayList;
import java.util.List;

import com.arnold.server.model.base.BaseEnterpriseVillageRelation;
import com.huntersun.tool.Utils;
import com.jfinal.plugin.activerecord.Page;

/**
 * Generated by JFinal.
 */
@SuppressWarnings("serial")
public class EnterpriseVillageRelation extends BaseEnterpriseVillageRelation<EnterpriseVillageRelation> {
	public static final EnterpriseVillageRelation dao = new EnterpriseVillageRelation();

	private static final String tableName = "tb_enterprise_village_relation";

	public static EnterpriseVillageRelation findBy(String enterpriseId, String villageId) {
		return dao.findFirst("select * from " + tableName + " where enterpriseId=? and villageId=? and isValid=?",
				enterpriseId, villageId, 1);
	}

	public Page<EnterpriseVillageRelation> pageEnterpriseVillageRelation(String villageId, String enterpriseId, Integer pageNumber,
			Integer pageSize) {
		String selectSQL = "SELECT * ";
		String fromSQL = " FROM " + tableName;

		String whereSQL = "";
		List<Object> paras = new ArrayList<Object>();

		if (!Utils.isBlankOrEmpty(villageId)) {
			if (Utils.isBlankOrEmpty(whereSQL)) {
				whereSQL = "WHERE";
			}
			whereSQL += " AND villageId=?";
			paras.add(villageId);
		}
		if (!Utils.isBlankOrEmpty(enterpriseId)) {
			if (Utils.isBlankOrEmpty(whereSQL)) {
				whereSQL = "WHERE";
			}
			whereSQL += " AND enterpriseId=?";
			paras.add(enterpriseId);
		}

		String orderBy = " ORDER BY createTime DESC";
		return dao.paginate(pageNumber, pageSize, selectSQL, fromSQL + whereSQL + orderBy, paras.toArray());
	}
}
