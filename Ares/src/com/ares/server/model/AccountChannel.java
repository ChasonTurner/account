package com.ares.server.model;

import java.util.List;

import com.ares.server.model.base.BaseAccountChannel;

/**
 * Generated by JFinal.
 */
@SuppressWarnings("serial")
public class AccountChannel extends BaseAccountChannel<AccountChannel> {
	public static final AccountChannel dao = new AccountChannel();

	public static final String TABLE_NAME = "tb_account_channel";

	public static Integer findStatusByType(String account, Integer type) {
		AccountChannel channel = dao.findFirst("select * from " + TABLE_NAME + " where accountId=? and type=?", account,
				type);
		return channel.getStatus();
	}

	public static List<AccountChannel> findChannelByAccount(String accountId) {
		return dao.find("select * from " + TABLE_NAME + " where accountId=?", accountId);
	}
	
	public static AccountChannel findChannelByAccountAndType(String accountId,Integer type) {
		return dao.findFirst("select * from " + TABLE_NAME + " where accountId=? and type=?", accountId,
				type);
	}
	
	public static boolean setChannelStatus(String account, Integer type, Integer status) {
		AccountChannel channel = dao.findFirst("select * from " + TABLE_NAME + " where accountId=? and type=?", account,
				type);
		channel.setStatus(status);
		
		return true;
	}
}
